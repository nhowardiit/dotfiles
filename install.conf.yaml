- defaults:
    link:
	relink: true

- clean: ['~']

- link:    
    ~/.bash:
    ~/.bash_logout:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.dotfiles: ''
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.oh-my-zsh/custom: omzCustom 
	force: true
    ~/.profile:
    ~/.pythonrc:
    ~/.shell:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:
	force: true

- shell:
        - [git submodule update --init --recursive, Installing submodules]

